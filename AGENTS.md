# ReviewScope AI 仓库协作规则

本文件用于指导在本仓库中工作的 AI 编程助手。开始任何开发任务前，必须先阅读本文件、`README.md`、`AI_USAGE.md` 和 `DEVELOPMENT_LOG.md`。

## 1. 项目目标

本项目是 LAIEN 校招 Homework。目标是交付一个可本地运行的 UI，将真实美国区 App Store 评论转换为：

```text
Review → Topic → Finding → Requirement / PRD → Test Case
```

所有重要结论必须能够追溯到真实评论。不得为了让演示结果更漂亮而伪造评论、数量、证据或模型结果。

## 2. 每次开始工作的固定流程

AI 在修改代码前必须依次执行：

1. 阅读 `README.md`、`AI_USAGE.md` 和 `DEVELOPMENT_LOG.md`。
2. 执行 `git status --short --branch`，识别用户已有修改，不覆盖无关文件。
3. 使用 UTF-8 读取中文文件；终端乱码不代表源文件损坏，先用 Python UTF-8 读取验证。
4. 执行当前基线测试：

   ```powershell
   .\.venv\Scripts\python.exe -m unittest discover -s tests -v
   .\.venv\Scripts\python.exe -m py_compile app.py
   ```

5. 在 `DEVELOPMENT_LOG.md` 中确认当前阶段、下一任务和验收标准。
6. 先说明本次准备完成什么，再开始修改。

## 3. 开发优先级

必须按照以下顺序推进，除非用户明确改变优先级：

1. P0：可运行入口、示例数据、CSV/JSON 导入、清洗与统计。
2. P0：动态主题发现、评论分类、Evidence Finding。
3. P0：引用校验、版本规划、PRD、测试用例、追溯矩阵。
4. P0：美国区 App Store 评论采集及失败降级。
5. P0：缓存 Demo、README、完整启动验证。
6. P1：导出、阶段重试、冲突证据、质量评分和更多测试。
7. P2：动画、多模型切换、复杂部署等非核心功能。

在 P0 闭环完成前，不实现登录、多 Agent、向量数据库、云队列或无关动画。

## 4. AI 与确定性代码的职责

Python 负责：

- 文件读取、字段验证和去重；
- 评论数量、比例、评分和版本统计；
- Review/Finding/Requirement/Test Case ID 校验；
- 追溯覆盖率和测试覆盖率；
- 过滤或阻止无证据的下游结果。

大模型负责：

- 动态发现当前数据中的主题；
- 理解评论语义和冲突观点；
- 草拟 Finding、版本规划、PRD 和测试用例；
- 输出符合 Pydantic/JSON Schema 的结构化数据。

禁止让模型自行生成统计数字，禁止接受未经代码验证的引用 ID。

## 5. 代码修改规则

- 优先进行小而完整、可验证的修改，不一次重写整个项目。
- 用户已有修改属于用户，不得覆盖或删除。
- 所有文本文件使用 UTF-8。
- API Key 只允许保存在 `.env` 或本地 secrets 中，不得提交。
- 新增功能必须包含失败提示；模型失败时不得继续生成没有证据的下游内容。
- 新增核心逻辑必须增加或更新测试。
- 不为示例 App 写死“订阅、训练、广告”等分类。
- 缓存结果必须来自真实运行，并记录数据来源、抓取时间、模型和限制。

## 6. 每次完成任务后的固定流程

1. 运行与修改相关的测试；核心阶段完成时运行全部测试。
2. 启动或编译检查入口文件，确认程序没有语法错误。
3. 检查 `git diff`，确认没有 Key、密码、Cookie 或无关文件。
4. 更新 `DEVELOPMENT_LOG.md`：完成项、测试结果、问题、下一步。
5. 如本次使用 AI 产生了重要设计或代码，更新 `AI_USAGE.md` 的使用记录。
6. 向用户说明：完成了什么、如何验证、当前风险、下一步建议。

## 7. Git 提交规则

用户已经要求 AI 辅助提交代码。AI 可以在一个独立阶段完成且验证通过后创建提交，但必须遵守：

- 只提交本项目 `app-review-insights/` 范围内与当前任务相关的文件。
- 不提交父目录中的其他项目、输出、虚拟环境或用户无关修改。
- 提交前运行测试并检查暂存区。
- 一次提交表达一个清楚的目的。
- 不伪造日期、提交历史或开发过程。
- 不推送到 GitHub，除非用户明确要求推送；提交与推送是两个动作。

推荐提交格式：

```text
chore: initialize ...
feat: add ...
fix: prevent ...
test: cover ...
docs: explain ...
```

如果父级 Git 仓库包含大量无关未跟踪文件，先提醒用户当前风险，仅暂存本项目明确相关文件。

## 8. 主动提醒规则

在当前对话和开发过程中，AI 应在以下节点主动提醒用户：

- 即将添加超出 P0 的功能时，提醒截止时间和范围风险。
- 实时采集尚未完成时，提醒保留 CSV/JSON 和缓存降级路径。
- API Key 可能被提交时，立即停止并提醒。
- 完整闭环尚未打通却开始美化页面时，提醒先完成闭环。
- 一个阶段完成时，提醒用户亲自运行并用自己的话解释。
- 提交前，提醒检查 README、缓存 Demo、敏感信息和新环境启动。
- Demo 前，提醒准备实时路径、缓存路径和 8 分钟讲解。

AI 不能在对话关闭后自动发送消息；如需定时提醒，需要另外创建 Codex 自动化或日历提醒。

## 9. 当前完成标准

只有同时满足以下条件，才能将 Homework 标记为完成：

- 项目可以按照 README 在新环境启动；
- 美国区真实评论可以采集或透明降级；
- 中间过程和最终结果都能在 UI 查看；
- PRD 需求可以追溯到具体评论；
- 测试用例可以追溯到 Requirement 和评论；
- 无效引用和无证据结论被自动阻止或明确标记；
- 没有 API Key 或网络时仍能加载真实缓存结果；
- 测试通过，Git 历史和 AI 使用记录真实完整。
